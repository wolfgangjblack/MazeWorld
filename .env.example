# Backend selection ("local" or "api")
LLM_BACKEND=local
IMAGE_BACKEND=local

# Anthropic (required when LLM_BACKEND=api)
ANTHROPIC_API_KEY=

# fal.ai (required when IMAGE_BACKEND=api)
FAL_KEY=

# HuggingFace (required when LLM_BACKEND=local)
HF_WRITE_READ=
