repos:
  - repo: local
    hooks:
      - id: pytest
        name: run tests
        entry: python3 -m pytest tests/ -v
        language: system
        stages: [pre-push]
        pass_filenames: false
        always_run: true
